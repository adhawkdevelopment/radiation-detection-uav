This directory contains the file that is uploaded to the microcontroller used for data collection, the specific microcontroller used as an Arduino Uno.  More specifically, the microcontroller collects data from an accelerometer, a geiger counter, and a gyroscope.  The microcontroller then wirelessly transmits the data to the user terminal.
